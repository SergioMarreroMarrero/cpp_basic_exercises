#include <cstdlib>
#include <iostream>
#include "utils.h"

using namespace std;

int main(){
    float x = 5.0;
    float y = 8.0;
    float d = 12;
    aritmetic_expression(x, y);
    grav_force(x, y, d);
    system("PAUSE");
    return 0;
}
